# @synclair/core (skeleton)

The machinery half of the north-star split (`docs/north-star.md`, Phase 3).
`docs/core-boundary.md` enumerates exactly what moves here and the three
reviewed steps. Nothing is imported from this package yet — the name and
registry are parked owner decisions; the boundary and the harness came first
so the move is a `git mv` under green CI, not an act of faith.
