# <Area> spec (distilled)

> Copy this to `<area>.md` (e.g. `change-orders.md`) when distilling a source.
> Keep it tight and decision-focused — it's a digest, not the raw PRD.

## Purpose
<What this view/feature is for, in one or two lines.>

## Requirements
- **Must:** <hard requirement>
- **Nice-to-have:** <optional>

## Entities & states
- **Entities:** <objects + key fields>
- **States:** <lifecycle; empty / loading / error>

## Roles & context
<Who uses it (PM, super, foreman, field crew, accounting, sub, owner) and where (office desktop vs. field mobile).>

## Rules & edge cases
<Contractual/financial rules, permissions, GC↔sub visibility, retainage, approvals.>

## Deviations from the norm
<Where this product intentionally differs from the industry standard, and why (one line each).>

## Source
<Link back to the manifest entry — PRD/deck/Figma source of record. Note read date.>
