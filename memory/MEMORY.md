# Memory index

Non-obvious **durable project facts** — external IDs, provenance, tool quirks —
readable by every agent brand (the router points here). One line per entry,
linking to a sibling `memory/*.md` file with the details. Entries reflect what
was true when written; verify before relying on file/flag names.

SEED: starts empty. Add entries as facts are learned, e.g.:

<!--
- [Figma workspace map](figma-workspace.md) — team/project IDs, how to enumerate files
- [Theme provenance](theme-provenance.md) — which Figma nodes the theme values came from
-->
